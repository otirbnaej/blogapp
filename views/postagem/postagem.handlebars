<div class="card">
    <div class="card-body">
        <h1 align="center">{{postagem.titulo}}</h1>
        <hr>
        <small>Data de publicação: {{postagem.data}}</small>
        <hr>
        {{postagem.conteudo}}

    </div>
</div>